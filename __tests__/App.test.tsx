import React from 'react';
import { render } from '@testing-library/react';

import App from '../src/App';

describe('<App />', () => {
  test('it should render App component', async () => {
    test.todo;
  });
});
